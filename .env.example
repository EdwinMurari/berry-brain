# Only needed for server mode. Copy to .env and set your own private paths.
BERRY_CONFIG_DIR=/absolute/private/config/berry-memory
BERRY_DATA_DIR=/absolute/private/data/berry-memory
BERRY_LLM_BASE_URL=https://gateway.example.org/v1
# Optional exact Gateway model ID. Empty disables background proposals.
BERRY_BRAIN_MODEL=
